<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daniel.dao.BookImageDAO">
    <insert id="add" useGeneratedKeys="true" keyProperty="id" parameterType="com.daniel.pojo.BookImage" >
        insert into bookimage ( bid ) values (#{book.id})
    </insert>

    <delete id="delete" parameterType="com.daniel.pojo.BookImage" >
        delete from bookimage where id= #{id}
    </delete>

    <delete id="deleteByBookId" parameterType="_int" >
        delete from bookimage where bid= #{bid}
    </delete>

    <select id="get" parameterType="_int" resultType="com.daniel.pojo.BookImage">
        select * from bookimage where id= #{id}
    </select>

    <update id="update" parameterType="com.daniel.pojo.BookImage" >
        update bookimage set bid=#{book.id} where id=#{id}
    </update>

    <select id="list" resultType="com.daniel.pojo.BookImage">
        select * from   bookimage
    </select>

    <select id="count" resultType="_int">
        select count(id) from bookimage
    </select>

    <!--<select id="getByBookId" resultType="com.daniel.pojo.BookImage">
        select * from bookImage where bid = #{bid} limit #{start},#{count}
    </select>-->

    <select id="getByBookId" parameterType="int" resultType="com.daniel.pojo.BookImage">
        select * from bookimage where bid = #{bid}
    </select>
</mapper>